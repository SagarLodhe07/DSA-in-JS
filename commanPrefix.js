// Example 1:

// Input: strs = ["flower","flow","flight"]
// Output: "fl"
// Example 2:

// Input: strs = ["dog","racecar","car"]
// Output: ""
// Explanation: There is no common prefix among the input strings.
const loangestCommanPrefix = (str) => {
  const a = str.map((word) => console.log(word));
  // return a;
};

console.log(loangestCommanPrefix(["flower", "flow", "flight"]));
